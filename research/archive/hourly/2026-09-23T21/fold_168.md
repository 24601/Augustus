## 168. Hourly 1531 HIGH (2026-09-23 ~15:31 Boise / 2026-09-23T21:31Z)

Folded onto `main` `bc0593617c413cd042f58a52274e89c48b216f8d`. Gap after the
1234 watch (§167), about three hours. Mode NEW_OFF_MAIN. This fold is §168
only. Do not reclaim §165, §166, or §167. Composition ranges ended at 996
with §163. §164 through §167 assigned none, and this fold does not reopen
that sequence.

Research-only: no skill, human README, Pages, or package version change.
Scoring rows stay at 17. Archer stays promised_not_landed. The 27
research-only watch rows were copied into this hour's archive and were not
fetched. Retrieval for the GitHub and Hub calls below is 2026-09-23T21:44Z.
Third-party numbers stay *theirs*. No X calls. No Harbor numbers were
invented. This run does not prove an unattended scheduler.

### Acceptance

**Problem.** An agent could treat a public-231 accuracy, a composite rank,
or a sealed accuracy as the same decision; treat a Choice on a fair coin or
a stated rate as a calibrated probability; treat a single model verdict as
the act; treat a fuzzy combination of marginals as a joint; or block on a
fine-tune that missed its own gate.

**Baseline.** Installed guidance already separates a leaderboard from a
deployment evaluation, refuses to treat a softmax or a training recipe as
calibration on this population, keeps the act in code or with a person,
refuses to combine marginals into a joint without a dependence assumption,
and requires an abstention owner. A typed output is not truth. A public
slice is not Harbor. TypeSafe Jev stays the default hosted exemplar for the
class, not the class. Mappings section 17 stays the last scoring section.

**Proposed improvement.** None in runtime files. Archive the sources' own
limits where they already match those rules, and record the identity joins.

**Assumptions.** A README opening, a card opening, a tool-reference page, or
a commit compare is enough to refuse promotion. None of those is a
reproduction. A description rewrite with an unchanged tree is not a new
capability review. A moved SHA is a review trigger, not a new family.

**Affected surfaces.** This section, the hourly archive, fingerprints for
sources actually looked at, `sources.json` pointers, and the hourly logs.
Not `SKILL.md`, not `README.md` below `## License`, not the site.

**Checks.** `make check`. No Pages build. No behavioral scenario run,
because installed guidance did not change.

**Counterexample that would reject this disposition.** A held-out run, on
labels and traffic this repository controls, where the composite rank, the
public-231 accuracy, and the sealed accuracy are one decision. A fair-coin
Choice that is uniform on that same population. A checkpoint that missed
its stated gate and is still safe to block on. A new score row that does
not change place, refuse, or baseline would also reject a promotion.

### A composite rank is not the sealed decision

**allebee/jevk5** densify §165 and §167. Node `R_kgDOUlwTMQ`. HEAD
`708556582fb419e9038e2f7f654f2805daddc4ba` to
`01d3cc99e11d5a080d9d15dd232eaa89e0941c1d`. Description hash
`062f51928c07` to `3e5e821a3552`. Three commits, ahead only. Compare
inspected for `README.md` and `MODEL_CARD.md`. Demo media was not opened.
Benches were not rerun.

Their new text, *theirs:* JevBench v1.4 still ranks JevK5 v0.2 second of 76
at 62.04 against 63.29 for Jev 1.13.0. On 308 fresh sealed decisions the
same published aggregates report 33.1% for JevK5 and 36.7% for Jev, against
85.3% and 86.6% on the 231 public decisions. They say the sealed set is
unusually difficult, that small sealed differences do not prove pairwise
superiority, and that the composite mixes intelligence, calibration, speed,
and cost. Workflow quality on Jev's published tasks is unmeasured. The
readout is SemIf's option-letter softmax on Qwen3.5-4B. The server
serializes questions. English only, at most 16 options, refuse past 16,384
tokens. The Snake clip is not a Jev comparison. The ticket clip is a paced
replay. Qwen3.5 is not Archer. Do not merge the composite, the sealed
accuracy, and the public-231 table. Do not add a scoring row. §165 and
§167 stand.

**hf:alibiserikbay/JevK5-2B**, sha
`fc7e3031a77e91b6de76004825d4d8b6a8947680`. Card opening. Same training
story on `Qwen/Qwen3.5-2B`, letter-logit readout, temperature 1.42 in
`jevk5_config.json`. The held-out table under that opening was not copied.
**hf:alibiserikbay/JevK5-GGUF**, sha
`a4bdf0651513b7d28c9f03f583abf80530a3fafe`, names both `JevK5` and
`JevK5-2B` as base models. Packaging is not another model. Qwen3.5 is not
Archer.

**clawdbot58-pixel/sentinel**, node `R_kgDOUoBM6Q`, HEAD
`bda4269f3224a57b241a40440b9119598ba48f78`. README banner. `docs/` was not
opened. *Theirs, public JevBench-231:* 87.9% accuracy and score 77.0,
against 86.6% and 75.4 for hosted Jev, ECE 0.049 at temperature 1.2, on a
Qwen3.8-27B letter-logit proxy. A Snake fill of 36/36 sits in that README
as a case study. Public 231 is not Harbor. Qwen3.8 is not Archer. Not
`harshwasan/jev-sentinel`.

**Mushroom-Systems/lichen**, HEAD
`ef4a6e395f30c6e02f86013e2682cf977a5d6220`. README through the public
table. Harness not rerun. Label-token readout, prompt written twice, option
order averaged. *Theirs, public items, gemma-4-26B-A4B QAT:* 48/48 easy,
71/72 standard, 88/111 hard, accuracy 0.896, median 147 ms, against Jev
1.13.0 at 81/111 hard, accuracy 0.866, median 665 ms. They say the official
score also uses held-out items plus calibration, speed, and cost, so this
table is the public part only. Averaging already requires requalification.
Gemma 4 is not Archer. Wire compatibility is not logit equivalence.

**artalis-io/jev-bush**, HEAD
`27bc68fda1101a3d55cf3601b109d701d992d2c0`. README opening. Benchmark not
rerun. Answer-slot logits from DiffusionGemma, softmax, no free-form
generation. *Theirs:* strongest run 67.40% on a pinned public set of 2,000
decisions, 5.3 points under a published 72.70% for Jev 1.13.0. They say
that is argmax quality on this benchmark, not model equivalence.

### The act stays outside the score

**kinde-starter-kits/jev-agent-authorization** densify §166. The watch
tagged it novel. Node `R_kgDOUmiakw` is unchanged. HEAD
`5e938cb7bb8d5d9417566b35e9b9e3a41f5ba22b` to
`48819c20f4a05cfd5b5b7d925466e55820ade55a`. Description hash unchanged
`5c5140014e9b`. Twenty-two commits. README read, including the benchmark
table. `convex/guard/handler.ts` and `convex/bench/` were not re-read.
The bench was not rerun.

*Theirs, 300 labeled calls, three repeats, three deciders.* False-allow
and accuracy: policy code on Jev signals 0.0% and 83.7%; a single Jev
verdict 12.4% and 71.3%; an LLM judge 7.9% and 77.6%. Those labels are
theirs. A 0.0% false-allow on that file is not a safety certificate.
Permission still denies before the model. A failed ledger write blocks the
call. Low confidence goes to an LLM judge in their README. §166's note
that the thresholds were unvalidated constants stays in force until a run
on labels this repository controls says otherwise.

**iDiagoValeta/jev-decision-gate**, HEAD
`72e21da73e426fd4081e9dfea55888b967104fd3`, tag `v0.3.0`. README opening.
The `decision` answer wins at any confidence. Safe and risk answers are
logged and do not veto. Errors fail open to a person, not to a silent
allow. A kill-list rejects catastrophic shell patterns before the model.
They say `opencode run --auto` bypasses the gate, including that kill-list.
Host policy still owns the act.

**WINEEL/jev-scheduling-evaluator**, HEAD
`116ac0981df8b07f12d37e1e66904a21a898878c`. README opening. People, numbers,
and scenarios are invented. Hard constraints are assumed already solved in
ordinary code. Jev sees soft questions. Python applies explicit thresholds.
Equal assignment totals are not a good schedule in their synthetic case.

**JackBeerman/swarm**, HEAD
`364bdaa9d3046754d4055a7fdb5ff2d1c0ddd096`. README through the slow-lane
question list. `docs/FINDINGS.md` was not opened. Status line: paused
2026-09-23. Nothing trades on a schedule. Four real orders, all tiny and
manual: one won, three lost, and they say those three were placed on the
wrong side of a correct signal, -$3.99. Edge is not demonstrated. The
cheap tier filters spend. That cascade is already the placement. No
trading rule added.

**brida-ai/reflexbench**, HEAD
`077d9d560263b8ac02da53c228189b664a542c6c`, tag `v1.0.0`. README headline.
`RESULTS.md` was not opened. Runs were not repeated. *Theirs:* Jev raw
semantic accuracy 96.4% (106/110) to 100% (110/110) after a deterministic
policy on the same response, 4 rescues, 0 harms, 0 extra model calls. A
blind replication 76.7% raw to 82.0% on 150/150, paired delta +5.3 points,
bootstrap 95% +1.3 to +10.0. They keep an earlier blind gate as FAIL
because one provider 503 left it at 149/150. They say they do not publish
one best-model score. Code can tighten a frozen response. The table is not
Harbor.

**vij-sameerb5/JevX**, HEAD
`e7338214eaae407ac284f609a29b9a3bca251041`, tag `v0.4.0`. README opening.
Their default writes a Jev call when a fit is at least 70%, keeps the old
rule as fallback, and reverts on failing tests. 70% is their cutoff. Not
`umgbhalla/jevx`.

**boriscardano/herdr-jev-router**, HEAD
`485e9c9a7a772a7919305087d85f5092340c8d60`, tag `v0.1.3`. README opening.
Advisory. A direct `herdr agent start` still bypasses the router. Routing
is not execution.

**manish-9245/Wayfinder**, HEAD
`fc646050d42d836a8af23e73545f565d0a7ee7b8`. README opening. Zero model
code. A verdict is their threshold policy around Laya. "No hallucination"
and about 33 ms are banner sentences. Not measured here.

**Avinash-jetwani/jevmem**, HEAD
`842d440d5bd3a42ae25cba4761486b9f96bfe46a`, tag `v0.4.3`. README opening.
A memory line can store a confidence comment. The line is not permission
to act. Not `libingzheren/Jev-Mem`.

### Calibration claims stay on their population

**MohitSV/jev-calibration-audit**, node `R_kgDOUn73Xg`, HEAD
`f589639442c8860de0cb0a6097852acfe309cd4d`. README findings list. Experiment
notes and JSONL were not rerun. Not `jujumilk3/jev-calibration-audit`.

*Theirs:* with no evidence, Choice puts 0.83 to 0.93 on the word "heads"
for a fair coin, and the pull mostly vanishes after one weak witness. On a
stated 55/45 base rate, Choice goes to 0.90 to 0.996, and they say
temperature scaling cannot recover it. Noul tracks the same targets to
within 0.015 to 0.027. Asking one Noul per option and normalizing cuts
their held-out Brier from 0.067 to 0.003. That normalization is a repair
on their exact-target file. It does not define a joint for a later
population. A fair-coin prompt is still not a fair bit (§163). Do not
replace Choice with normalized Nouls as a general rule.

**r4topunk/jev-trading-study**, HEAD
`2d0c4e63be9ad76a71c0b47d265791cfb118b463`. README result table.
`EXPERIMENT.md` was not opened. Backtest was not rerun. The description's
40,228 is not the scored count in this README. *Theirs, 15-minute horizon:*
8,624 decisions on each of ETH, cbBTC, SOL, and VIRTUAL, 34,496 pooled,
AUC 0.475 with interval 0.468 to 0.482. They say the score follows recent
momentum and the pools mean-revert, and that a positive control leaks to
AUC 1.000. They also say the served version cannot be pinned and repeats
vary by up to 0.04. AUC is ordering, not a calibrated probability, and a
win on neither is a trading rule. Keep the baseline when it wins.

**zaesho/S1Rank**, HEAD `c532159e74379177d85d5bea46aa884d7b2752e3`. README
headline and method list. Cache was not recomputed. Pinned `jev-1.13.0`,
probabilities rounded to two decimals, September 2026 serving. *Theirs:*
one request holding 100 documents, one Noul each, beats a single "which is
best" Choice on the sets they print (DL19 joint pointwise nDCG@10 73.7
against Choice-best 65.6, BM25 50.6). Calibration depends on prevalence:
ECE 0.023 on TREC-COVID, overconfident where relevant documents are rare,
and a recalibration does not transfer across collections. 52% of
probabilities change across byte-identical requests. S1Adaptive is their
negative result: calibrated uncertainty tracks query hardness, not which
queries another reranker would handle better. Ranking quality and
calibration stay different questions. The table is not Harbor.

**stillmarcus24/jev-verify**, HEAD
`7a161eeef676b995ee7146de687d6fc5e7ce9cec`. README through the identity
list. Census script was not rerun. They attribute Choice confidence
`C = (N * p_max - 1) / (N - 1)` to Stanislav Yurin, 18 September 2026, and
say primeline replicated it. That is the same margin the typesafe-mcp
tool reference writes down for Choice, and the same shape §167 recorded
for Manchego's own field. Question design already says Jev's Choice
confidence comes from the option distribution and is not maximum
probability or measured accuracy. The live TypeSafe confidence page was
not re-fetched this hour.

Their Score row applies that identity too. The typesafe-mcp tool reference
says TypeSafe does not publish a Score formula. Both sentences stay theirs.
Do not pick one without the current contract page.

Census, *theirs, not rerun:* 279,842 published answers, 270,872 conform
(96.79%). They warn that independent per-option probabilities can sum past
1 without being a forgery, and that a decimal collision between score and
confidence is common at two places. `afshinm/laya-mps` uses normalized
entropy `1 - H/log(n)`. They say it reorders 9.96% of 199,497 pairs
against Jev's margin, so a threshold does not transfer. An adapter that
renames the field is already the pijev example in question design. Of 58
projects they called Jev-compatible, 51 publish no answers to check.

**itsmostafa/typesafe-mcp** densify §142. Node `R_kgDOUeU9Ww`. HEAD
`d4c110c7edd82127a4ca962c9d60fb96f748eb6e` to
`4f29b32b2e3eb6e2eb20673b31d3745ea5c5ead4`. Description hash
`7e3449dd8b7d` to `4924e14f7649`. Release tag `v0.4.5`. Forty-five commits.
README and `docs/tool-reference.md` read. Tests were not run.

The tool passes API probabilities through, reorders them to criteria
order, and applies `min_confidence` locally. For Choice that threshold
uses the API confidence. For Noul it uses `|2p - 1|`, which the page says
is the two-outcome case, and the API still returns no Noul confidence
field. `items` is their fan-out, at most 500 records and 8 requests at
once, not a TypeSafe batch field. `TYPESAFE_BASE_URL` can point at a local
server. A local route is not logit equivalence. "Real probabilities" is
the README banner. Not `PyModel/typesafe-mcp`. Not `cyrusasco/typesafe-mcp`.

**hf:wwydmanski/bielik-minitron-jev-v0.2**, sha
`7b04d30a62cc0387aa29e17fd98ecb32da8b0f92`. Card opening. `any2jev` LoRA
on `speakleash/Bielik-Minitron-7B-v3.0-Instruct`, temperature 1.632 fitted
on validation. The card also says "0.0 M trainable parameters" beside
rank 64. Do not treat 0.0 M as a measurement. They say calibration is the
validation split, not a per-answer guarantee. Not the v0.1 card in §167.
Bielik is not Archer.

### A missed gate stays a missed gate

**hf:Jojoarumugam/laya-agentguard**, sha
`e255b93865605420bc7bd7efacadadf572e6d01e`, base
`convaiinnovations/laya`. Card opening. The numeric rows under the holdout
heading were not copied. The card says the checkpoint missed its own
recipe, 0.85 accuracy and 0.10 ECE, on both questions, on a hand-written
holdout scored once. It tells the reader to keep the checkpoint in shadow
before letting it block. Those two targets are their recipe, not a
universal bar.

**devjothish/laya-forge**, HEAD
`b9463185d15c15574b5a10c410548756b5a1549b`, tag `v1.0.0`. README opening.
The published agentguard checkpoint is the example that fails the forge
gate. *Theirs, one first test:* stock Laya rated `echo 'rm -rf /' >>
notes.txt` destructive at 0.78 and `terraform apply -auto-approve` against
production safe at 0.15. About 25 ms on an M4 Pro is their sentence. Not
measured here. A keyword miss is why the gate exists. It does not make the
failed checkpoint a blocker.

### Question, proof, and readout identity

**narphorium/nl-logic-interpreter**, HEAD
`3970f092223c64c9569625f87ff995d0859fc6ad`. README opening. SLD resolution
with Jev as the unifier. They say a local model can bind a rule variable
to "day" instead of the person, so the example proves nothing. A model
unifier does not turn the trace into a classical proof. Proof keeps its
own semantics.

**dsaad68/fuzzy-jev**, HEAD
`f838addf287676c565231c07432ba8390d899595`, tag `v0.2.0`. README opening.
`docs/rules.md` was not opened, so the AND/OR/NOT formulas were not
checked. The command sends Choice, Score, and Noul through OpenRouter's
decisions endpoint and feeds the answers to a rules file. "Calibrated" is
their banner. Marginals still do not define a joint.

**jack-michaud/self-bootstrapping-categorizer**, HEAD
`55c26c447ded76d9de0e1e132206bf376378aae8`. README opening. Choice and
per-task Nouls are separate questions. They say a low Noul means likely
nonmembership and a middle value means ambiguity. The scalar does not
license that split. They also say there is no final consistency pass, and
that confident judgments do not establish correctness.

**apscot/The-Duel**, HEAD
`cac0fa8f0fa493d9e2df46b1fa395aa67a6449cc`. README opening. Same state,
same deadline, first answer acts inside a game. "Why Jev wins" is one
recorded session. A clock is not correctness.

**GTC6244/Laya-Decision**, HEAD
`58286c72378668fda586173942949119ec125f07`, tag `v0.1.1`. README opening.
Rust candle port of `NandhaKishorM/laya`. They say the candle backend
matches PyTorch within 1e-4 on the published checkpoint. Not rerun.
"Calibrated confidence" is their sentence. `POST /v1/systemone` is a wire
server. Not a new family.

**aydinozturk/LayaMCP**, HEAD
`86f04b20eaca951a1fb116f18421a27b5d0c9941`. README opening. Description
rewrite. `laya_route` runs no model. About 35 ms per question on a T4 is
theirs, not measured. "Calibrated probability" is their sentence.

**mcembalest/sys1**, HEAD
`bfb3575751a8c06cd03941d8a55f24f925814e83`. README opening. Rust candle
server for Laya decision heads. About 14 ms per query on an RTX Pro 6000
is theirs, not measured. The watch description attributes the base to
`alvarobartt/sys1`. Histories were not compared.

**qzqdz/jevtree**, HEAD `5ccfee24b5c158f0e644514c08311edb272fd311`. README
opening. An LLM builds a feature table and information gain grows a tree.
The smoke path is a loan CSV. Not `lee-lou2/jev-tree`. The tree was not
rebuilt.

**ZF-Utokyo/Jev-Benchmark**, HEAD
`57b42a356de9369fa07f9a67bb224228e9c4047b`. README is a website pointer.
*Theirs, in that README:* 10 models, 123 test contracts, 30 fixed anchors,
4,830 recorded attempts. The site and the cost-accuracy image were not
opened. "Same score, different decisions" is already the Score rule. No
result cell was copied. Not Harbor.

### Card densify and empty trees

**hf:tasksource/tasksource-jev-typed-decisions** densify §167. SHA
`35f5abfb3d2c6e0db6226bf17aeeec3a7b208882` to
`987cc2a7cb8156eebd51cc62029443eb95719a77`. Card README read.
`release-audit.json` was not opened. Parquet was not loaded. The card
still says 900,000 train, 50,000 validation, and 50,000 test. Later
commits on this day are card prose: breadth wording and a link to the
Tasksource catalog. License remains `other`. A tagged procedural component
remains. BIG-bench, MMLU, and BLiMP are named as absent. The per-task
mapping is still unaudited. A recast target is not automatically the
original task's gold. The first 1,000 training rows are interleaved for
the viewer. That order adds no rows.

Hub API only, cards not opened: `hf:rupeshs/laya-ov-int8` sha
`472075756b1dbc4d73b54f47440acd80dcad5b59` (description rewrite, OpenVINO
quant). `hf:Infin8-AI/laya` sha
`e9012d95a9af8ff33cddf5745ab6003a4b5b2557` (text-classification tag).
`hf:JonesLin/next-jev-stage1-cot` sha
`8fd8c0867084fa47e91d89a9bdac887e68310a25`, base `Qwen/Qwen3.5-0.8B`.
Not the stage2 id left unread in §167. `hf:vtava/Laya-MemoryFusion-V3`
sha `92d12e7651262454bae2deb75dd8916a16488c54` (text-generation tag, not
read as a decision head). `hf:fukayatti0/jev-japanese-judgment` sha
`e1d012f62412b6549132ff12bec6c2ae397ba077`, base
`LiquidAI/LFM2.5-1.2B-JP-202606`. Description rewrite. Do not promote any
of these on tags alone. Qwen3.5 is not Archer.

Empty repositories, no HEAD invented. **ToufiqQureshi/maayaa**, node
`R_kgDOUoA5GA`. The description's order-stability abstention is not a
tree. **blacksinisterx/jev-guard**, node `R_kgDOUoGU4A`. Not the other
`jev-guard` cards.

**PyModel/jev-judge-mcp**, HEAD
`81aedb658d04a113aac8f28a101f577c5b634ae9`. Metadata only. Not
`itsmostafa/typesafe-mcp`. Not `PyModel/typesafe-mcp`.

### Other first looks at metadata depth

Repo metadata only for the rest of the 88. Not a code audit. Live star
counts are in `run_digest.json`. They are not a quality score.

The 27 research-only ids are names in the packet only.

### Decision-design card

```text
Domain, desired behavior, decision owner: unchanged; code or a person owns the act
Current baseline and why a model might help: leaderboard slices, calibration scope, marginal combination, and abstention already cover this packet
Pillar, placement, and family: no new family; letter-logit readouts, LoRA heads, candle ports, and wire servers stay inside existing families
Evidence source, freshness, candidate coverage, missing/contradictory states: 2026-09-23T21:44Z README and card openings plus two commit compares; benches not rerun; 27 research-only rows not fetched; Score confidence formula disagrees between jev-verify and typesafe-mcp
Judgments and output semantics; what remains exact or generated: composite rank is not sealed accuracy; public 231 is not Harbor; Choice on a fair coin is not that coin; Noul scalar is not a nonmembership proof
Policy, costs/utility, constraints, authority, and execution checks: unchanged; a single verdict is not the policy arm; a missed forge gate stays in shadow; a game clock is not correctness
Abstention/error behavior and fallback owner: unchanged; fail-open to a person is a policy; a bypass flag can skip the kill-list
Batchable versus dependent steps: unchanged; items fan-out and joint rerank state are wrappers, not new joints
Model, adapter/aggregation, rubric, candidate-source, calibration, policy versions: record SemIf versus margin versus entropy confidence, and the sealed versus public instruments, when those wrappers are actually used
Development/calibration/test split and label provenance: Tasksource row counts unchanged in the card; procedural slice still tagged; kinde 300 and S1Rank cache stay theirs
Falsifier, metrics, acceptable risk/coverage, and evaluation artifact: one instrument on our labels that makes the composite, the public slice, and the sealed slice the same decision; a fair-coin Choice that is uniform; a missed-gate checkpoint that is safe to block on
Implementation/evaluation entry points; search budget and confirmation plan: none this hour
Observed result, limitations, and next decision: research-only; next experiment only if a placement needs one of these wrappers on labels we control
```

Soft judgment is not the veto. The sources' own limits and the existing
rules are. `invented_signal: false`.
