# Execution provenance (not a response grade)

Source: `origin/research/080-exopo-trainer`, detached checkout of
[`5b7610b`](https://github.com/24601/Augustus/commit/5b7610b767c9373cf2d0d25983f0e913f736caae).
Worker: [behavior journey thread](https://ampcode.com/threads/T-01a0dea3-8139-72cd-9086-24469140a8bc).
Execution date: 2026-09-26 UTC. Local runtime: Python 3.11.6 in a Linux orb.
The runtime model identity is not exposed to this worker; no independent reviewer
or nested worker was invoked.

## Source and skill loading

Initial shell command, successful:

```sh
git fetch origin research/080-exopo-trainer
git checkout --detach 5b7610b767c9373cf2d0d25983f0e913f736caae
cat CONTRIBUTING.md
cat research/protocol.md
```

Tool calls in order: `skill(name="augustus")`, `reload_skills()` (83 skills,
no errors), `skill(name="augustus-train")`. The Augustus loader returned the
workspace skill and its companion-training routing text. Reload preceded loading
the trainer, whose returned body began “Deliver a runnable task specialist and
its evidence, or retain a better incumbent.” No skills were edited or installed
by this worker. The parent's separate installation smoke result is not a run
performed by this worker.

Read the candidate trainer's five references with:

```sh
cat .agents/skills/augustus-train/references/{data-and-splits,compute-envelope,fit-and-serve,improve-and-confirm,spec-defects}.md
cat Makefile
git status --short
```

`skill-sha256.txt` contains actual SHA-256 hashes for all 30 tracked skill files,
generated by `git ls-files .agents/skills | sort | xargs sha256sum` after checkout.
It is an inventory, not a claim that all 30 files were read. The two entry hashes:

- Augustus: `76e8002fb98936ca608025dddfa71c8c0356a13301f38792c951eb2b31aaf325`
- Trainer: `8e9a2977ad6cf58120a5df3bacf71fa30387b7645acea209f81f447c0d3d99b4`

## Verification and limits

`execution.txt` records traced commands and raw outputs: all six router tests
passed, including a fresh isolated Python process using only a copied source file;
the eight-line CLI replay returned approved routes and review results and recovered
after malformed JSON. Tests are synthetic contract fixtures, not customer outcomes.
The application has no third-party dependency.

The first repository checker attempt failed because PyYAML was absent. Installed
the pinned development dependency in `/tmp/augustus-behavior-check`, then reran
successfully; `check-retry.txt` preserves both the installation and checker output.

Did not run full `make check`: a targeted filename-reference search found that
`tests/test_plugin_evals.py` loads the prohibited scenario file. To keep this fresh
response uncontaminated, neither that file's scenarios/expected notes nor the
behavioral-review rubric was read, and the full suite was left for the parent.
The check performed was the repository structural checker plus the new application
suite and whitespace check, not a claim of full-suite success.

`responses.md` is the authored raw application response plus seven distinct advisory
responses; those advisory proposals were not executed as model experiments.
`prompts.txt` preserves task wording by case; `request.txt` preserves the complete
initial request body. Source code and all evidence live only under this behavior
directory. No external deployment, push or merge. Local commit only. No semantic
self-grading was performed.
