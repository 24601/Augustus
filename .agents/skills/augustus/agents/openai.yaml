interface:
  display_name: "Augustus"
  short_description: "Design and evaluate bounded model judgments"
  default_prompt: "Use $augustus to find one useful decision-model placement, keep authority explicit, and propose a test that could reject it."
